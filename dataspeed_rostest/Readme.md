# Dataspeed ROS test
Adds several useful handlers for patterns found in Dataspeed testing done with gtest/rostest

See `eol_tester_btpe/src/tester.cpp` and `eol_tester_qcan/src/tester.cpp` for examples
